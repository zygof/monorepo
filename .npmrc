# Hoist @types/* packages au root node_modules pour que TypeScript les trouve
# depuis n'importe quel package du monorepo (nécessaire avec transpilePackages)
public-hoist-pattern[]=@types/*
public-hoist-pattern[]=eslint*
